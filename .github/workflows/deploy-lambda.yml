name: Deploy Lambda (reusable)

on:
  workflow_call:
    inputs:
      dir:
        required: true
        type: string
      environment:
        required: true
        type: string

jobs:
  deploy:
    name: ${{ inputs.dir }}
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    concurrency:
      group: deploy-${{ inputs.dir }}
      cancel-in-progress: false
    permissions:
      id-token: write
      contents: read
    env:
      DIR: ${{ inputs.dir }}
      ENVFILE: ${{ inputs.dir }}/../.env
    steps:
      - uses: actions/checkout@v4

      - name: Check definitions
        run: |
          set -euo pipefail
          [ -f "$DIR/function.json" ] || { echo "::error::${DIR}/function.json not found"; exit 1; }
          [ -f "$ENVFILE" ] || { echo "::error::${ENVFILE} not found"; exit 1; }
          {
            echo "### ${{ inputs.dir }}${{ vars.DRY_RUN == 'true' && ' — DRY RUN, not deployed' || '' }}"
            echo '```'; grep -v '^#' "$ENVFILE"; echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - uses: fujiwara/lambroll@v1
        with:
          version: v1.5.3

      - name: Dry run (render only)
        if: vars.DRY_RUN == 'true'
        run: |
          echo "::warning::DRY_RUN is set on environment '${{ inputs.environment }}': rendering only, nothing is deployed"
          scripts/render.sh "$DIR"

      - name: Check credentials
        if: vars.DRY_RUN != 'true'
        env:
          AWS_ROLE_ARN: ${{ vars.AWS_ROLE_ARN }}
          AWS_REGION: ${{ vars.AWS_REGION }}
        run: |
          for v in AWS_ROLE_ARN AWS_REGION; do
            [ -n "${!v}" ] || { echo "::error::variable ${v} is not set on GitHub Environment '${{ inputs.environment }}'"; exit 1; }
          done

      - uses: aws-actions/configure-aws-credentials@v4
        if: vars.DRY_RUN != 'true'
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: lambroll diff / deploy
        if: vars.DRY_RUN != 'true'
        run: |
          set -euo pipefail
          l() { lambroll --envfile "$ENVFILE" "$@" --function "$DIR/function.json"; }
          l diff || true
          l deploy
